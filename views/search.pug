extends layouts/main.pug
include mixins/videoBlock

block content
    .search__header
        h3 Searching for #{searchKeyword}
    .search_videos
        each video in videos
            +videoBlock(video)
